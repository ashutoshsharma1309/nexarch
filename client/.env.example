# ── NexArch client environment ─────────────────────────────────────────────
# The client calls the API at the same origin under /api (proxied by Vite in
# development and nginx in production), so no variable is required to run.
# VITE_API_BASE_URL exists as an escape hatch for split-origin deployments.

# VITE_API_BASE_URL=
