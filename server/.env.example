# ── NexArch API environment ───────────────────────────────────────────────
# Copy to `.env` and adjust. Every variable is validated at boot by
# src/shared/config/env.ts — the process refuses to start on invalid config.

# Runtime
NODE_ENV=development
PORT=4000
LOG_LEVEL=debug

# Database (MySQL via Prisma)
DATABASE_URL="mysql://nexarch:change-me@localhost:3306/nexarch"

# CORS — comma-separated list of allowed browser origins
CORS_ORIGINS=http://localhost:5173

# Rate limiting (per IP, per window)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# Auth (consumed in a later phase — validated now so deploys fail early,
# not on first login attempt). Generate with: openssl rand -base64 48
JWT_SECRET=dev-only-secret-change-me-please-0000000000000000
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
