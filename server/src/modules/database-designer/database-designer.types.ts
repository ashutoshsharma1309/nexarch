/**
 * The Database Designer's types.
 *
 * The design artifacts are the cross-stage pipeline contract and live in
 * shared/types/design.ts (Phase 5+ consume them). They are re-exported here
 * so this module's public surface is unchanged.
 */
export type * from '../../shared/types/design.js';
